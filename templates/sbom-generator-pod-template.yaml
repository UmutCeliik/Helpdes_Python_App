# templates/sbom-generator-pod-template.yaml (NİHAİ ve EN TEMİZ VERSİYON)
apiVersion: v1
kind: Pod
metadata:
  name: __POD_NAME__
  namespace: ado-agents
  labels:
    app: sbom-generator
spec:
  restartPolicy: Never
  serviceAccountName: ado-agent-sa
  securityContext:
    fsGroup: 1001

  initContainers:
  - name: git-cloner
    image: alpine:3.19 # Versiyon sabitlendi
    command: ["/bin/sh", "-c"]
    args:
    - |
      set -ex
      GIT_REPO_URL_WITH_TOKEN=$(echo "__GIT_REPO_URL__" | sed "s,https://,https://__AZP_TOKEN__@,")
      git clone "$GIT_REPO_URL_WITH_TOKEN" /workspace/source
    env:
    - name: GIT_REPO_URL
      value: "__GIT_REPO_URL__"
    - name: AZP_TOKEN
      value: "__AZP_TOKEN__"
    volumeMounts:
    - name: work-volume
      mountPath: /workspace

  containers:
  # Konteyner 1: Her proje tipi için aynı komutu çalıştırır
  - name: generator
    image: cyclonedx/cyclonedx-cli:0.28.2 # <-- Sizin bulduğunuz harika imaj ve sabitlenmiş versiyon! 
    workingDir: /workspace/source/__CONTEXT_SUB_PATH_RELATIVE__/
    command: ["/bin/sh", "-c"]
    args:
    - |
      set -ex
      echo ">>> Generating SBOM with cyclonedx-cli..."
      # Bu komut proje tipini (node, python vb.) otomatik algılar.
      cyclonedx-cli analyze . --output-file /workspace/sbom.json
      
      # Uploader konteynerinin devam etmesi için .done dosyasını oluştur
      touch /workspace/generator.done
      echo ">>> SBOM generation complete."
    volumeMounts:
    - name: work-volume
      mountPath: /workspace

  # uploader konteyneri aynı kalıyor
  - name: uploader
    image: curlimages/curl:8.9.0
    workingDir: /workspace/
    command: ["/bin/sh", "-c"]
    args:
    - |
      set -ex
      echo ">>> Uploader waiting for generator.done file..."
      while [ ! -f /workspace/generator.done ]; do sleep 2; done
      
      if [ -f "/workspace/sbom.json" ]; then
        echo ">>> SBOM file found. Uploading..."
        SBOM_FILE="sbom-$(date +%s)-__SERVICE_NAME__.json"
        mv /workspace/sbom.json "/workspace/$SBOM_FILE"
        curl -vS --fail -u "__NEXUS_USERNAME__:__NEXUS_PASSWORD__" \
            --upload-file "/workspace/$SBOM_FILE" \
            "http://nexus-repository-manager.nexus:8081/repository/sbom-reports/$SBOM_FILE"
        echo ">>> SBOM upload successful!"
      else
        echo ">>> No sbom.json found. Nothing to upload."
      fi
    volumeMounts:
    - name: work-volume
      mountPath: /workspace
      
  volumes:
  - name: work-volume
    emptyDir: {}